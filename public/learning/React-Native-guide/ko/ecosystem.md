# 생태계와 인기 라이브러리

내비게이션, UI, 폼, 애니메이션을 위한 인기 라이브러리에 익숙해지세요. 설치, 링크, 사용 방법을 알아두세요.

```tsx
import { Button } from 'react-native-paper';

<Button mode="contained" onPress={() => console.log('눌림')}>눌러주세요</Button>
```
*이것은 React Native Paper의 버튼을 사용합니다.*
