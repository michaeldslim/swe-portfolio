# Ecosystem and Popular Libraries

Familiarize yourself with popular libraries for navigation, UI, forms, and animations. Know how to install, link, and use them.

```tsx
import { Button } from 'react-native-paper';

<Button mode="contained" onPress={() => console.log('Pressed')}>Press me</Button>
```
*This uses a button from React Native Paper.*
